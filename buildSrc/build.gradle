buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.2.71"
    }
}

repositories {
    google()
    jcenter()
}

apply from: project.file("common.gradle")

dependencies {
    deps.each {
        implementation it
    }
}
